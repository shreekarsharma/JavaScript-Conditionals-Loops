let num = 351;
let lowerLimit = 15;
let upperLimit = 40;
if (num >= lowerLimit && num <= upperLimit) {
  console.log(num + " exists between " + lowerLimit + " and " + upperLimit);
} else {
  console.log(
    num + " does not exists between " + lowerLimit + " and " + upperLimit
  );
}
