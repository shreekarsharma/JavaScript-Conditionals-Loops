let char = "A";
temp = char.toLowerCase();
if (
  temp === "a" ||
  temp === "e" ||
  temp === "i" ||
  temp === "o" ||
  temp === "u"
) {
  console.log(char + " is a vowel");
} else {
  console.log(char + " is a consonent");
}
